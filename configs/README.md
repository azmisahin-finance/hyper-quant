# Configuration

Configuration files must contain non-secret defaults only.

Use environment variables or a dedicated secret-management mechanism for credentials. Never commit real API keys, private keys, seed phrases, or signer material.
