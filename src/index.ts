/**
 * HYPER-QUANT implementation entrypoint.
 *
 * v2.6 is still a review candidate. Runtime implementation is intentionally
 * kept minimal until the specification review and promotion gates are closed.
 */
export const HYPER_QUANT_SPEC_STATUS = 'REVIEW_REQUIRED' as const;
export const HYPER_QUANT_SPEC_VERSION = '2.6' as const;
